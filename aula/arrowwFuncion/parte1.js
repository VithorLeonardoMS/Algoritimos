/*
Tipo de função com sintaxe simplificada
Por ser uma expreção, deve ser atribuída a uma varável para ser invocada
Invocação continua a mesma
*/
const calculaArea = (altura, lagura) => {
    const area = altura * largura
    return area
}
const areaCalculada = calculaArea(2,3)
